# 新增模块衔接问题与处理记录

## 总览

- 识别问题总数：9
- 已处理：8
- 暂缓：1（按当前决策，后续在 Docker 环境验证）

## 问题明细

| 编号 | 问题 | 影响 | 处理方法 | 状态 |
|---|---|---|---|---|
| 1 | 项目原有数据访问以 `mysql.connector + 原生 SQL` 为主，缺少统一 ORM 层 | 用户、积分、下载记录难以统一管理 | 新增 `database` 模块，拆分为 `models/session/service`，并在 API 启动时执行建表初始化 | 已处理 |
| 2 | 缺少用户/积分/下载记录对应 API | 新功能无法被前端或外部服务调用 | 新增用户 upsert、用户查询、积分调整、下载记录创建、下载记录分页查询接口 | 已处理 |
| 3 | Dockerfile 依赖 `requirements.txt`，但仓库中缺失 | 镜像构建直接失败 | 新增 `requirements.txt` 并补齐当前代码所需依赖 | 已处理 |
| 4 | Docker 编排中没有 MySQL 服务 | 新增数据模块无法在容器内落库 | 在 `docker-compose.yml` 中新增 `mysql` 服务、健康检查、持久化卷、初始化 SQL 挂载 | 已处理 |
| 5 | 应用与数据库启动有先后竞态 | API 可能先启动后连库失败 | 在 `docker-entrypoint.sh` 增加 MySQL 可达性等待，再启动 API/前端 | 已处理 |
| 6 | 新旧表结构存在字段断层（旧逻辑依赖 `users.permissions/video_records`） | 旧脚本可能出现字段不存在错误 | 在 SQLModel 的 `User` 模型补齐 JSON 字段映射，兼容旧脚本读取路径 | 已处理 |
| 7 | upsert 覆盖语义不安全 | 仅传部分字段时，旧值可能被清空 | 调整更新逻辑为“仅更新非 `None` 字段” | 已处理 |
| 8 | ORM 对象返回含 datetime，存在序列化风险 | 新接口可能出现 500 响应 | 使用 `model_dump(mode="json")` 输出，保证返回体可 JSON 序列化 | 已处理 |
| 9 | 本机安装依赖出现权限错误（WinError 5） | 本机运行级联调受阻 | 按当前决策暂缓本机验证，后续改为 Docker 环境执行运行验证 | 暂缓 |

## 额外稳态处理

- 对解析主流程做降级保护：解析成功后即使“解析次数统计写库失败”，也不影响 `/api/parse` 成功响应。
- 数据库连接串改为 `SQLAlchemy URL.create` 构造，避免密码中特殊字符导致连接串解析失败。

## 当前结论

- 新增模块已可与旧部分衔接，核心冲突点已消除。
- 运行级端到端验证按当前安排放到 Docker 环境执行。
